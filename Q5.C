// 5 -  Faça uma função para a inserção de nome, que será utilizada para inserir o nome do funcionário e do dependente;

void inserirNome(char *nome, char *texto){
    printf("%s\n", texto);
    scanf("%s", nome);
    return;
};